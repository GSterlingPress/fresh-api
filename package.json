{
  "name": "fresh-api",
  "version": "0.1.2",
  "private": true,
  "type": "module",
  "description": "Shared URL freshness intelligence for AI agents.",
  "engines": {"node": ">=20"},
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test test/*.test.js"
  }
}
